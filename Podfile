# Uncomment the next line to define a global platform for your project
platform :ios, '12.1'

target 'HeroInterview' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for HeroInterview

  pod 'Alamofire'
  pod 'SwiftyJSON'
  pod 'Reusable'
  pod 'Kingfisher'

  target 'HeroInterviewTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'HeroInterviewUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
